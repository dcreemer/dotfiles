{{- $email := promptString "email" -}}
{{- $location := promptString "location? (home|work|other)" -}}
{{- $profile := promptString "profile? (basic|full)" -}}
[data]
    email = "{{ $email }}"
    location = "{{ $location }}"
    profile = "{{ $profile }}"

[secret]
    command = "1pass"

encryption = "gpg"
[gpg]
    recipient = "{{ $email }}"